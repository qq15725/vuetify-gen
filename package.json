{
  "scripts": {
    "build": "lerna run build --scope vuetify-gen --stream",
    "docs:serve": "lerna run serve --scope vuetifyjs-gen.com --stream",
    "docs:build": "lerna run build --scope vuetifyjs-gen.com --stream"
  },
  "devDependencies": {
    "lerna": "^3.15.0"
  }
}
